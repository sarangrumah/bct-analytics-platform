# dbt run artefacts. target/ holds compiled SQL and run_results.json; logs/
# holds dbt.log. Both are per-run output, both are large, and both are
# regenerated by the next `dbt build`.
target/
logs/
dbt_packages/
.user.yml
