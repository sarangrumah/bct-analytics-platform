<?xml version="1.0" encoding="utf-8"?>
<!--
    GENERATED by scripts/generate-search-views.py. Do not hand-edit:
    re-running the generator overwrites this file.

    To customise one of these views, move its record into a normal view
    file in this module. The generator only emits views for models that
    have none, so a moved record is left alone from then on.
-->
<odoo>
    <data>

        <record id="custom_event_sponsor_view_search" model="ir.ui.view">
            <field name="name">custom.event.sponsor.search</field>
            <field name="model">custom.event.sponsor</field>
            <field name="arch" type="xml">
                <search string="Custom Event Sponsor">
                    <field name="name"/>
                    <separator/>
                    <filter name="inactive" string="Archived" domain="[('active', '=', False)]"/>
                    <group>
                        <filter name="groupby_currency_id" string="Currency" domain="" context="{'group_by': 'currency_id'}"/>
                        <filter name="groupby_event_id" string="Event" domain="" context="{'group_by': 'event_id'}"/>
                        <filter name="groupby_tier" string="Tier" domain="" context="{'group_by': 'tier'}"/>
                    </group>
                </search>
            </field>
        </record>

    </data>
</odoo>
