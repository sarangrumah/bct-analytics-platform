<?xml version="1.0" encoding="utf-8"?>
<!--
    GENERATED by scripts/generate-search-views.py. Do not hand-edit:
    re-running the generator overwrites this file.

    To customise one of these views, move its record into a normal view
    file in this module. The generator only emits views for models that
    have none, so a moved record is left alone from then on.
-->
<odoo>
    <data>

        <record id="helpdesk_sla_view_search" model="ir.ui.view">
            <field name="name">helpdesk.sla.search</field>
            <field name="model">helpdesk.sla</field>
            <field name="arch" type="xml">
                <search string="Helpdesk Sla">
                    <field name="name"/>
                    <separator/>
                    <filter name="inactive" string="Archived" domain="[('active', '=', False)]"/>
                    <group>
                        <filter name="groupby_priority" string="Applies to Priority" domain="" context="{'group_by': 'priority'}"/>
                    </group>
                </search>
            </field>
        </record>

        <record id="helpdesk_tag_view_search" model="ir.ui.view">
            <field name="name">helpdesk.tag.search</field>
            <field name="model">helpdesk.tag</field>
            <field name="arch" type="xml">
                <search string="Helpdesk Tag">
                    <field name="name"/>
                    <separator/>
                    <filter name="inactive" string="Archived" domain="[('active', '=', False)]"/>
                </search>
            </field>
        </record>

        <record id="helpdesk_team_view_search" model="ir.ui.view">
            <field name="name">helpdesk.team.search</field>
            <field name="model">helpdesk.team</field>
            <field name="arch" type="xml">
                <search string="Helpdesk Team">
                    <field name="name"/>
                    <separator/>
                    <filter name="inactive" string="Archived" domain="[('active', '=', False)]"/>
                    <group>
                        <filter name="groupby_alias_id" string="Alias" domain="" context="{'group_by': 'alias_id'}"/>
                        <filter name="groupby_default_priority" string="Default Priority" domain="" context="{'group_by': 'default_priority'}"/>
                        <filter name="groupby_sla_id" string="Default SLA" domain="" context="{'group_by': 'sla_id'}"/>
                    </group>
                </search>
            </field>
        </record>

    </data>
</odoo>
