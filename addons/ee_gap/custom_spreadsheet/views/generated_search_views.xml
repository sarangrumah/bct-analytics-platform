<?xml version="1.0" encoding="utf-8"?>
<!--
    GENERATED by scripts/generate-search-views.py. Do not hand-edit:
    re-running the generator overwrites this file.

    To customise one of these views, move its record into a normal view
    file in this module. The generator only emits views for models that
    have none, so a moved record is left alone from then on.
-->
<odoo>
    <data>

        <record id="custom_spreadsheet_tag_view_search" model="ir.ui.view">
            <field name="name">custom.spreadsheet.tag.search</field>
            <field name="model">custom.spreadsheet.tag</field>
            <field name="arch" type="xml">
                <search string="Custom Spreadsheet Tag">
                    <field name="name"/>
                </search>
            </field>
        </record>

        <record id="custom_spreadsheet_version_view_search" model="ir.ui.view">
            <field name="name">custom.spreadsheet.version.search</field>
            <field name="model">custom.spreadsheet.version</field>
            <field name="arch" type="xml">
                <search string="Custom Spreadsheet Version">
                    <field name="note"/>
                    <group>
                        <filter name="groupby_saved_at" string="Saved At" domain="" context="{'group_by': 'saved_at'}"/>
                        <filter name="groupby_saved_by" string="Saved By" domain="" context="{'group_by': 'saved_by'}"/>
                        <filter name="groupby_workbook_id" string="Workbook" domain="" context="{'group_by': 'workbook_id'}"/>
                    </group>
                </search>
            </field>
        </record>

    </data>
</odoo>
