<?xml version="1.0" encoding="utf-8"?>
<!--
    GENERATED by scripts/generate-search-views.py. Do not hand-edit:
    re-running the generator overwrites this file.

    To customise one of these views, move its record into a normal view
    file in this module. The generator only emits views for models that
    have none, so a moved record is left alone from then on.
-->
<odoo>
    <data>

        <record id="knowledge_article_version_view_search" model="ir.ui.view">
            <field name="name">knowledge.article.version.search</field>
            <field name="model">knowledge.article.version</field>
            <field name="arch" type="xml">
                <search string="Knowledge Article Version">
                    <group>
                        <filter name="groupby_article_id" string="Article" domain="" context="{'group_by': 'article_id'}"/>
                        <filter name="groupby_saved_at" string="Saved At" domain="" context="{'group_by': 'saved_at'}"/>
                        <filter name="groupby_saved_by" string="Saved By" domain="" context="{'group_by': 'saved_by'}"/>
                    </group>
                </search>
            </field>
        </record>

        <record id="knowledge_tag_view_search" model="ir.ui.view">
            <field name="name">knowledge.tag.search</field>
            <field name="model">knowledge.tag</field>
            <field name="arch" type="xml">
                <search string="Knowledge Tag">
                    <field name="name"/>
                    <separator/>
                    <filter name="inactive" string="Archived" domain="[('active', '=', False)]"/>
                </search>
            </field>
        </record>

    </data>
</odoo>
