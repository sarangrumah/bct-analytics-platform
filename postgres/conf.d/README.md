# Machine-local Postgres overrides, untracked.
# postgresql.conf ends with:  include_if_exists = '/etc/postgresql/conf.d/local.conf'
# so anything dropped here as local.conf overrides every setting above it.
